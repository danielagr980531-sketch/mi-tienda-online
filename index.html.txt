<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda Online - Productos Varios</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Variables de colores */
        :root {
            --primary: #6C9BCF;
            --secondary: #A5C0DD;
            --accent: #4A8BDF;
            --light: #F0F8FF;
            --dark: #2C3E50;
            --success: #4CAF50;
            --text: #333333;
            --text-light: #666666;
            --white: #FFFFFF;
            --shadow: rgba(0, 0, 0, 0.1);
        }

        /* Estilos generales */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--light);
            color: var(--text);
            line-height: 1.6;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        /* Header y navegación */
        header {
            background-color: var(--white);
            box-shadow: 0 2px 10px var(--shadow);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary);
        }

        .logo i {
            color: var(--accent);
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 25px;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--text);
            font-weight: 500;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .cart-icon {
            position: relative;
            cursor: pointer;
        }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background-color: var(--accent);
            color: var(--white);
            border-radius: 50%;
            width: 18px;
            height: 18px;
            font-size: 0.7rem;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: var(--white);
            padding: 80px 0;
            text-align: center;
        }

        .hero h1 {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }

        .hero p {
            font-size: 1.2rem;
            max-width: 700px;
            margin: 0 auto 30px;
        }

        .btn {
            display: inline-block;
            background-color: var(--accent);
            color: var(--white);
            padding: 12px 25px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
        }

        .btn:hover {
            background-color: var(--dark);
            transform: translateY(-3px);
        }

        /* Sección de productos destacados */
        .section-title {
            text-align: center;
            margin: 50px 0 30px;
            color: var(--dark);
        }

        .section-title h2 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .section-title p {
            color: var(--text-light);
            max-width: 600px;
            margin: 0 auto;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 50px;
        }

        .product-card {
            background-color: var(--white);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px var(--shadow);
            transition: transform 0.3s;
        }

        .product-card:hover {
            transform: translateY(-10px);
        }

        .product-img {
            height: 200px;
            width: 100%;
            background-color: var(--secondary);
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--white);
            font-size: 3rem;
        }

        .product-info {
            padding: 15px;
        }

        .product-title {
            font-weight: 600;
            margin-bottom: 5px;
        }

        .product-price {
            color: var(--accent);
            font-weight: 700;
            margin-bottom: 10px;
        }

        .product-rating {
            color: #FFC107;
            margin-bottom: 10px;
        }

        .product-actions {
            display: flex;
            justify-content: space-between;
        }

        .add-to-cart {
            background-color: var(--primary);
            color: var(--white);
            border: none;
            padding: 8px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .add-to-cart:hover {
            background-color: var(--accent);
        }

        /* Sección de productos relacionados */
        .related-products {
            background-color: var(--white);
            padding: 50px 0;
        }

        .related-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }

        .related-card {
            background-color: var(--light);
            border-radius: 10px;
            padding: 15px;
            text-align: center;
            transition: transform 0.3s;
        }

        .related-card:hover {
            transform: scale(1.05);
        }

        .related-img {
            height: 120px;
            width: 100%;
            background-color: var(--secondary);
            border-radius: 8px;
            margin-bottom: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--white);
            font-size: 2rem;
        }

        /* Sección de WhatsApp */
        .whatsapp-section {
            background-color: var(--dark);
            color: var(--white);
            padding: 50px 0;
            text-align: center;
        }

        .whatsapp-btn {
            background-color: #25D366;
            color: var(--white);
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 15px 30px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.2rem;
            margin-top: 20px;
            transition: all 0.3s;
        }

        .whatsapp-btn:hover {
            background-color: #128C7E;
            transform: scale(1.05);
        }

        /* Footer */
        footer {
            background-color: var(--dark);
            color: var(--white);
            padding: 40px 0 20px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 30px;
            margin-bottom: 30px;
        }

        .footer-column h3 {
            margin-bottom: 20px;
            font-size: 1.2rem;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 10px;
        }

        .footer-links a {
            color: var(--light);
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-links a:hover {
            color: var(--secondary);
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 15px;
        }

        .social-links a {
            color: var(--white);
            font-size: 1.2rem;
            transition: color 0.3s;
        }

        .social-links a:hover {
            color: var(--secondary);
        }

        .copyright {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 0.9rem;
            color: var(--light);
        }

        /* Modal de producto */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background-color: var(--white);
            width: 90%;
            max-width: 800px;
            border-radius: 10px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            border-bottom: 1px solid #eee;
        }

        .close-modal {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-light);
        }

        .modal-body {
            padding: 20px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .modal-product-info {
            display: flex;
            gap: 20px;
        }

        .modal-product-img {
            flex: 1;
            height: 250px;
            background-color: var(--secondary);
            border-radius: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--white);
            font-size: 4rem;
        }

        .modal-product-details {
            flex: 1;
        }

        .modal-product-title {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        .modal-product-price {
            font-size: 1.3rem;
            color: var(--accent);
            font-weight: 700;
            margin-bottom: 15px;
        }

        .modal-product-description {
            margin-bottom: 20px;
            color: var(--text-light);
        }

        .related-products-modal {
            margin-top: 20px;
        }

        .related-products-modal h3 {
            margin-bottom: 15px;
        }

        .related-mini-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            gap: 10px;
        }

        .related-mini-card {
            background-color: var(--light);
            border-radius: 8px;
            padding: 10px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
        }

        .related-mini-card:hover {
            background-color: var(--secondary);
            color: var(--white);
        }

        .related-mini-img {
            height: 60px;
            background-color: var(--primary);
            border-radius: 5px;
            margin-bottom: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--white);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .navbar {
                flex-direction: column;
                gap: 15px;
            }

            .nav-links {
                gap: 15px;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .modal-product-info {
                flex-direction: column;
            }

            .modal-product-img {
                height: 200px;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="container">
            <nav class="navbar">
                <div class="logo">
                    <i class="fas fa-store"></i>
                    <span>TiendaOnline</span>
                </div>
                <ul class="nav-links">
                    <li><a href="#">Inicio</a></li>
                    <li><a href="#">Productos</a></li>
                    <li><a href="#">Ofertas</a></li>
                    <li><a href="#">Contacto</a></li>
                </ul>
                <div class="cart-icon">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="cart-count">0</span>
                </div>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h1>Descubre Nuestros Productos Exclusivos</h1>
            <p>Encuentra todo lo que necesitas con la mejor calidad y al mejor precio. ¡Envíos a todo el país!</p>
            <a href="#featured" class="btn">Ver Productos</a>
        </div>
    </section>

    <!-- Productos Destacados -->
    <section id="featured" class="featured-products">
        <div class="container">
            <div class="section-title">
                <h2>Productos Destacados</h2>
                <p>Los productos más populares de nuestra tienda</p>
            </div>
            <div class="products-grid">
                <!-- Producto 1 -->
                <div class="product-card" data-id="1">
                    <div class="product-img">
                        <i class="fas fa-headphones"></i>
                    </div>
                    <div class="product-info">
                        <h3 class="product-title">Auriculares Inalámbricos</h3>
                        <div class="product-price">$89.99</div>
                        <div class="product-rating">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star-half-alt"></i>
                        </div>
                        <div class="product-actions">
                            <button class="add-to-cart">Añadir al Carrito</button>
                            <button class="btn-view-details">Ver Detalles</button>
                        </div>
                    </div>
                </div>

                <!-- Producto 2 -->
                <div class="product-card" data-id="2">
                    <div class="product-img">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <div class="product-info">
                        <h3 class="product-title">Funda para Móvil</h3>
                        <div class="product-price">$19.99</div>
                        <div class="product-rating">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="far fa-star"></i>
                        </div>
                        <div class="product-actions">
                            <button class="add-to-cart">Añadir al Carrito</button>
                            <button class="btn-view-details">Ver Detalles</button>
                        </div>
                    </div>
                </div>

                <!-- Producto 3 -->
                <div class="product-card" data-id="3">
                    <div class="product-img">
                        <i class="fas fa-laptop"></i>
                    </div>
                    <div class="product-info">
                        <h3 class="product-title">Soporte para Laptop</h3>
                        <div class="product-price">$34.99</div>
                        <div class="product-rating">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                        </div>
                        <div class="product-actions">
                            <button class="add-to-cart">Añadir al Carrito</button>
                            <button class="btn-view-details">Ver Detalles</button>
                        </div>
                    </div>
                </div>

                <!-- Producto 4 -->
                <div class="product-card" data-id="4">
                    <div class="product-img">
                        <i class="fas fa-clock"></i>
                    </div>
                    <div class="product-info">
                        <h3 class="product-title">Reloj Inteligente</h3>
                        <div class="product-price">$129.99</div>
                        <div class="product-rating">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star-half-alt"></i>
                        </div>
                        <div class="product-actions">
                            <button class="add-to-cart">Añadir al Carrito</button>
                            <button class="btn-view-details">Ver Detalles</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Sección WhatsApp -->
    <section class="whatsapp-section">
        <div class="container">
            <h2>¿Tienes dudas sobre algún producto?</h2>
            <p>Contáctanos por WhatsApp para confirmar tu pedido o resolver cualquier consulta</p>
            <a href="https://wa.me/1234567890?text=Hola,%20estoy%20interesado%20en%20uno%20de%20sus%20productos" class="whatsapp-btn" target="_blank">
                <i class="fab fa-whatsapp"></i>
                Contactar por WhatsApp
            </a>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>TiendaOnline</h3>
                    <p>Tu tienda de confianza para productos de calidad. Envíos a todo el país.</p>
                    <div class="social-links">
                        <a href="#"><i class="fab fa-facebook"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                    </div>
                </div>
                <div class="footer-column">
                    <h3>Enlaces Rápidos</h3>
                    <ul class="footer-links">
                        <li><a href="#">Inicio</a></li>
                        <li><a href="#">Productos</a></li>
                        <li><a href="#">Ofertas</a></li>
                        <li><a href="#">Contacto</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Información</h3>
                    <ul class="footer-links">
                        <li><a href="#">Sobre Nosotros</a></li>
                        <li><a href="#">Política de Envíos</a></li>
                        <li><a href="#">Términos y Condiciones</a></li>
                        <li><a href="#">Política de Privacidad</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Contacto</h3>
                    <ul class="footer-links">
                        <li><i class="fas fa-map-marker-alt"></i> Dirección: Calle Principal 123</li>
                        <li><i class="fas fa-phone"></i> Teléfono: +1 234 567 890</li>
                        <li><i class="fas fa-envelope"></i> Email: info@tiendaonline.com</li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; 2023 TiendaOnline. Todos los derechos reservados.</p>
            </div>
        </div>
    </footer>

    <!-- Modal de Producto -->
    <div class="modal" id="productModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Detalles del Producto</h2>
                <button class="close-modal">&times;</button>
            </div>
            <div class="modal-body">
                <div class="modal-product-info">
                    <div class="modal-product-img">
                        <i class="fas fa-box" id="modal-product-icon"></i>
                    </div>
                    <div class="modal-product-details">
                        <h3 class="modal-product-title" id="modal-product-title">Nombre del Producto</h3>
                        <div class="modal-product-price" id="modal-product-price">$0.00</div>
                        <div class="product-rating" id="modal-product-rating">
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="fas fa-star"></i>
                            <i class="far fa-star"></i>
                        </div>
                        <p class="modal-product-description" id="modal-product-description">Descripción del producto.</p>
                        <button class="add-to-cart" id="modal-add-to-cart">Añadir al Carrito</button>
                    </div>
                </div>
                <div class="related-products-modal">
                    <h3>Productos Relacionados</h3>
                    <div class="related-mini-grid" id="related-products-container">
                        <!-- Los productos relacionados se cargarán aquí dinámicamente -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Datos de productos
        const products = [
            {
                id: 1,
                title: "Auriculares Inalámbricos",
                price: "$89.99",
                icon: "fas fa-headphones",
                description: "Auriculares inalámbricos con cancelación de ruido y batería de larga duración. Perfectos para viajes y uso diario.",
                rating: 4.5,
                related: [2, 3]
            },
            {
                id: 2,
                title: "Funda para Móvil",
                price: "$19.99",
                icon: "fas fa-mobile-alt",
                description: "Funda protectora para móviles con diseño elegante y protección contra golpes. Disponible para diferentes modelos.",
                rating: 4,
                related: [1, 4]
            },
            {
                id: 3,
                title: "Soporte para Laptop",
                price: "$34.99",
                icon: "fas fa-laptop",
                description: "Soporte ergonómico para laptop que mejora la postura y facilita la ventilación del dispositivo.",
                rating: 5,
                related: [1, 4]
            },
            {
                id: 4,
                title: "Reloj Inteligente",
                price: "$129.99",
                icon: "fas fa-clock",
                description: "Reloj inteligente con monitor de actividad, notificaciones y resistencia al agua. Ideal para deportistas.",
                rating: 4.5,
                related: [2, 3]
            },
            {
                id: 5,
                title: "Altavoz Bluetooth",
                price: "$49.99",
                icon: "fas fa-volume-up",
                description: "Altavoz portátil con sonido de alta calidad y batería de hasta 12 horas de duración.",
                rating: 4,
                related: [1, 3]
            },
            {
                id: 6,
                title: "Cargador Rápido",
                price: "$24.99",
                icon: "fas fa-bolt",
                description: "Cargador rápido compatible con la mayoría de dispositivos móviles. Carga tu dispositivo en menos tiempo.",
                rating: 4,
                related: [2, 5]
            }
        ];

        // Elementos del DOM
        const modal = document.getElementById('productModal');
        const closeModal = document.querySelector('.close-modal');
        const modalTitle = document.getElementById('modal-product-title');
        const modalPrice = document.getElementById('modal-product-price');
        const modalIcon = document.getElementById('modal-product-icon');
        const modalDescription = document.getElementById('modal-product-description');
        const modalRating = document.getElementById('modal-product-rating');
        const relatedContainer = document.getElementById('related-products-container');
        const addToCartButtons = document.querySelectorAll('.add-to-cart');
        const cartCount = document.querySelector('.cart-count');
        let cartItems = 0;

        // Función para mostrar las estrellas de valoración
        function renderRating(rating) {
            const stars = Math.floor(rating);
            const halfStar = rating % 1 !== 0;
            let html = '';
            
            for (let i = 0; i < 5; i++) {
                if (i < stars) {
                    html += '<i class="fas fa-star"></i>';
                } else if (i === stars && halfStar) {
                    html += '<i class="fas fa-star-half-alt"></i>';
                } else {
                    html += '<i class="far fa-star"></i>';
                }
            }
            
            return html;
        }

        // Función para abrir el modal con los detalles del producto
        function openProductModal(productId) {
            const product = products.find(p => p.id === productId);
            if (!product) return;
            
            modalTitle.textContent = product.title;
            modalPrice.textContent = product.price;
            modalIcon.className = product.icon;
            modalDescription.textContent = product.description;
            modalRating.innerHTML = renderRating(product.rating);
            
            // Limpiar productos relacionados
            relatedContainer.innerHTML = '';
            
            // Cargar productos relacionados
            product.related.forEach(relatedId => {
                const relatedProduct = products.find(p => p.id === relatedId);
                if (relatedProduct) {
                    const relatedCard = document.createElement('div');
                    relatedCard.className = 'related-mini-card';
                    relatedCard.setAttribute('data-id', relatedProduct.id);
                    relatedCard.innerHTML = `
                        <div class="related-mini-img">
                            <i class="${relatedProduct.icon}"></i>
                        </div>
                        <div>${relatedProduct.title}</div>
                        <div>${relatedProduct.price}</div>
                    `;
                    relatedContainer.appendChild(relatedCard);
                    
                    // Agregar evento de clic a los productos relacionados
                    relatedCard.addEventListener('click', () => {
                        openProductModal(relatedProduct.id);
                    });
                }
            });
            
            modal.style.display = 'flex';
        }

        // Event listeners para los botones "Ver Detalles"
        document.querySelectorAll('.btn-view-details').forEach(button => {
            button.addEventListener('click', function() {
                const productId = parseInt(this.closest('.product-card').getAttribute('data-id'));
                openProductModal(productId);
            });
        });

        // Cerrar modal
        closeModal.addEventListener('click', () => {
            modal.style.display = 'none';
        });

        // Cerrar modal al hacer clic fuera
        window.addEventListener('click', (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });

        // Añadir productos al carrito
        addToCartButtons.forEach(button => {
            button.addEventListener('click', function() {
                cartItems++;
                cartCount.textContent = cartItems;
                
                // Efecto visual
                this.textContent = '¡Añadido!';
                this.style.backgroundColor = 'var(--success)';
                
                setTimeout(() => {
                    this.textContent = 'Añadir al Carrito';
                    this.style.backgroundColor = 'var(--primary)';
                }, 1500);
            });
        });

        // Inicializar valoraciones de productos
        document.querySelectorAll('.product-card').forEach(card => {
            const productId = parseInt(card.getAttribute('data-id'));
            const product = products.find(p => p.id === productId);
            if (product) {
                const ratingElement = card.querySelector('.product-rating');
                ratingElement.innerHTML = renderRating(product.rating);
            }
        });
    </script>
</body>
</html>